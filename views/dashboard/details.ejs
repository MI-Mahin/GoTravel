<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tour Package Details</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f2f2f2;
        margin: 0;
        padding: 20px;
      }

      h1 {
        color: #333;
        font-size: 24px;
        margin-bottom: 10px;
        text-align: center; /* Center the heading */
      }

      p {
        color: #666;
        margin-bottom: 5px;
      }

      form {
        margin-top: 20px;
        text-align: center; /* Center the form */
      }

      label {
        display: block;
        margin-bottom: 5px;
      }

      input[type="number"] {
        width: 100px;
        padding: 5px;
        border: 1px solid #ccc;
        border-radius: 3px;
      }

      button[type="submit"] {
        padding: 10px 20px;
        background-color: #333;
        color: #fff;
        border: none;
        border-radius: 3px;
        cursor: pointer;
      }

      #bookingConfirmation {
        margin-top: 20px;
        padding: 10px;
        background-color: #f9f9f9;
        border: 1px solid #ccc;
        border-radius: 3px;
        text-align: center; /* Center the booking confirmation message */
      }

      /* Center the image */
      .center-container {
        display: flex;
        justify-content: center;
      }

      .center-image {
        margin: auto;
        display: block;
      }
    </style>
  </head>
  <body>
    <h1><%= tourPackage.package_name %></h1>
    <!-- Center the image -->
    <div class="center-container">
      <img
        class="center-image w-1/3 h-auto object-cover"
        src="<%= tourPackage.image_url %>"
        alt="Package Image"
      />
    </div>

    <p>Location: <%= tourPackage.location %></p>
    <p>Tour Date: <%= tourPackage.tour_date %></p>
    <p>Max Capacity: <%= tourPackage.max_capacity %></p>
    <p>Description: <%= tourPackage.description %></p>
    <p>Price: <%= tourPackage.price %> <%= tourPackage.currency %></p>

    <form id="bookingForm" method="post">
      <input
        type="hidden"
        id="packageId"
        name="packageId"
        value="<%= tourPackage.package_id %>"
      />
      <label for="numberOfPeople">Number of People:</label>
      <input type="number" id="numberOfPeople" name="numberOfPeople" required />
      <button type="submit">Book Now</button>
    </form>

    <!-- Booking confirmation message -->
    <div id="bookingConfirmation" style="display: none">
      Booking confirmed! Thank you for choosing our tour package.
    </div>

    <a href="">Reserve</a>
    <!-- <script>
      const bookingForm = document.getElementById("bookingForm");
      const bookingConfirmation = document.getElementById(
        "bookingConfirmation"
      );

      bookingForm.addEventListener("submit", async function (event) {
        event.preventDefault(); // Prevent default form submission behavior

        // Get form data
        const packageId = document.getElementById("packageId").value;
        const numberOfPeople = document.getElementById("numberOfPeople").value;
        console.log(packageId, numberOfPeople);

        // Send AJAX request to add the tour package to user's bookings list
        try {
          // console.log(req.userInfo);

          const response = await fetch("/mybookings", {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
            body: JSON.stringify({
              packageId,
              numberOfPeople,
            }),
          });

          if (response.ok) {
            // Display booking confirmation message
            bookingConfirmation.style.display = "block";
          } else {
            console.error("Failed to book tour package:", response.statusText);
          }
        } catch (error) {
          console.error("Failed to book tour package:", error.message);
        }
      });
    </script> -->
  </body>
</html>
