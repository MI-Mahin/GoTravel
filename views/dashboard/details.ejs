<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script
      src="https://kit.fontawesome.com/c1df782baf.js"
      crossorigin="anonymous"
    ></script>
    <link rel="stylesheet" href="styles.css" />
    <title>Tour Package Details</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        min-height: 50vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: hsl(137, 46%, 24%);
        font-family: "Poppins", sans-serif;
        background-color: #f2f2f2;
        margin: 0;
        padding: 20px;
        position: relative;
      }
      .card {
        max-width: 33rem;
        background: #fff;
        margin: 0 1rem;
        padding: 1rem;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
        width: 100%;
        border-radius: 0.5rem;
        text-align: center;
        position: relative;
      }
      .star {
        font-size: 2rem;
        color: gold;
      }
      h1 {
        color: #333;
        font-size: 24px;
        margin-bottom: 10px;
      }
      p {
        color: #666;
        margin-bottom: 5px;
      }
      form {
        margin-top: 20px;
        text-align: center;
      }
      label {
        display: block;
        margin-bottom: 5px;
      }
      input[type="number"] {
        width: 100px;
        padding: 5px;
        border: 1px solid #ccc;
        border-radius: 3px;
      }
      button[type="submit"] {
        padding: 10px 20px;
        background-color: #333;
        color: #fff;
        border: none;
        border-radius: 3px;
        cursor: pointer;
      }
      #bookingConfirmation {
        margin-top: 20px;
        padding: 10px;
        background-color: #f9f9f9;
        border: 1px solid #ccc;
        border-radius: 3px;
        text-align: center;
      }
      .center-container {
        display: flex;
        justify-content: center;
      }
      .center-image {
        margin: auto;
        display: block;
        max-width: 100%;
        border-radius: 0.5rem;
      }
      .rating-container {
        position: absolute;
        bottom: 10px;
        left: 10px;
        text-align: left;
      }
      .rating-text {
        font-size: 14px;
        color: #333;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h1><%= tourPackage.package_name %></h1>
      <div class="center-container">
        <img
          class="center-image"
          src="<%= tourPackage.image_url %>"
          alt="Package Image"
        />
      </div>
      <p>Location: <%= tourPackage.location %></p>
      <p>Tour Date: <%= tourPackage.tour_date %></p>
      <p>Max Capacity: <%= tourPackage.max_capacity %></p>
      <p>Description: <%= tourPackage.description %></p>
      <p>Price: <%= tourPackage.price %> <%= tourPackage.currency %></p>

      <form id="bookingForm" method="post">
        <input
          type="hidden"
          id="packageId"
          name="packageId"
          value="<%= tourPackage.package_id %>"
        />
        <label for="numberOfPeople">Number of People:</label>
        <input
          type="number"
          id="numberOfPeople"
          name="numberOfPeople"
          required
        />
        <button type="submit">Book Now</button>
      </form>
      <div id="bookingConfirmation" style="display: none">
        Booking confirmed! Thank you for choosing our tour package.
      </div>

      <!-- Ratings Display -->
      <div class="rating-container">
        <div class="rating-text">User Rating: <%= rating %> / 5</div>
        <div class="stars">
          <% for(let i = 1; i <= 5; i++) { %> <% if(i <= rating) { %>
          <span class="star">&#9733;</span>
          <% } else { %>
          <span class="star">&#9734;</span>
          <% } %> <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
